import React from "react";
import { Template } from "../../templates";

const Dashboard = () => {
  return (
    <Template layout={true}>
      <div>Dashboard</div>
    </Template>
  );
};

export default Dashboard;
